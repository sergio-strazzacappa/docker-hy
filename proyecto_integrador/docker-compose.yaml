services:
  backend:
    image: backend:v1
    build: ./example-backend
    environment:
      - REDIS_HOST=redis
    ports:
      - "8080:8080"

  frontend:
    image: frontend:v1
    build: ./example-frontend
    ports:
      - "5001:5001"

  redis:
    image: redis
